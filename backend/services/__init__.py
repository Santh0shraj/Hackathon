# Purpose: Services package (business logic).
# Holds service modules that implement core logic, call models, and are used by routes.
# Keeps routes thin and delegates validation, DB access, and external APIs to services.
